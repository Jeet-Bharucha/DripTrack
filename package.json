{
  "name": "driptrack",
  "version": "1.0.0",
  "description": "DRIPTRACK — Real prices. Real products.",
  "scripts": {
    "start": "node backend/server.js",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
